# Inbox

Unsorted captures — drop anything here that doesn't yet have an obvious domain. Triage
during a session: move each item into the right `domains/<domain>/raw/` or `knowledge/`
once its domain is clear, or promote it to a new domain if a pattern emerges.
